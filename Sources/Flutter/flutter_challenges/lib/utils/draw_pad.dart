// import 'package:flutter/material.dart';

// _drawLeftCenter(Canvas canvas, Size size, Paint paint) {
//   final leftCenter = Offset(
//     _size.width / 2 - (buttonLength * 1.5),
//     _size.height - 56.0 - (buttonLength * 1.5),
//   );

//   canvas.drawCircle(
//       leftCenter, buttonLength, paint..color = Colors.yellow.withOpacity(0.5));
// }

// _drawTopCenter(Canvas canvas, Size size, Paint paint) {
//   final topCenter = Offset(
//     _size.width / 2,
//     _size.height - 56.0 - (buttonLength * 3),
//   );

//   canvas.drawCircle(
//       topCenter, buttonLength, paint..color = Colors.green.withOpacity(0.5));
// }

// _drawRightCenter(Canvas canvas, Size size, Paint paint) {
//   final rightCenter = Offset(
//     _size.width / 2 + (buttonLength * 1.5),
//     _size.height - 56.0 - (buttonLength * 1.5),
//   );

//   canvas.drawCircle(
//       rightCenter, buttonLength, paint..color = Colors.red.withOpacity(0.5));
// }

// _drawBottomCenter(Canvas canvas, Size size, Paint paint) {
//   final bottomCenter = Offset(
//     _size.width / 2,
//     _size.height - 56.0,
//   );

//   canvas.drawCircle(bottomCenter, buttonLength, paint);
// }
